---
title: Rollback
description: Restore system to pre-ArizenOS state
tasks:
  - !writeStatus: {status: 'ArizenOS — Running rollback...'}
  - !powerShell:
      command: '.\scripts\rollback.ps1'
      exeDir: true
      wait: true
      runas: currentUserElevated
  - !writeStatus: {status: 'ArizenOS — Rollback complete'}
